mixin proposal-gallery
  #proposal-gallery.proposal-gallery.carousel.slide(data-ride='carousel')&attributes(attributes)
    block

mixin proposal-gallery-items
  .carousel-inner(role='listbox')
    block
